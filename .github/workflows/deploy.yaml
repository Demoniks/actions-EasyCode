name: "EasyCode Deploy"

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest

    env:
      NAME: CodigoFacilito

    steps:
      - name: Working dir
        run: ls -al

      - name: Action
        uses: actions/checkout@v4

      - name: Working dir after action
        run: ls -al

  second_job:
    needs: first_job
    runs-on: ubuntu-latest

    steps:
      - name: Hello again
        run: echo "Hello again ${{ github.ref }}"
